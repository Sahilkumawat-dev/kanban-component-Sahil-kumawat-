== Kanban Board Component ==

A React + TypeScript Kanban Board documented with Storybook.

Features
-React + TypeScript + Storybook setup
-Modular components (Board, Column, Task, Modal)
-Responsive design
-ESLint + strict TS setup

Run Locally
```bash
npm install
npm run storybook
